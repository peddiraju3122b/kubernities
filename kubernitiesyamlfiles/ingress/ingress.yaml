---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spc-svc 
  annotations:
    imageregistry: https://hub.docker.com/repository/docker/peddiraju3122b/spc    
spec:
  rules:
    - http: 
        paths:
          - path: https://hub.docker.com/repository/docker/peddiraju3122b/spc
            backend:
              service:
                name: spc-svc
                port: 
                  number: 8080
            pathType: Exact
